# Cloud Run環境変数設定
# 本番環境で使用する環境変数をここに記載

env_variables:
  STREAMLIT_SERVER_HEADLESS: "true"
  STREAMLIT_SERVER_ADDRESS: "0.0.0.0"
  STREAMLIT_SERVER_PORT: "8080"
  STREAMLIT_CLIENT_SHOW_SIDEBAR_NAVIGATION: "false"
  
  # APIキー（Secret Managerを使用することを推奨）
  # GEMINI_API_KEY: "your-api-key-here"